<UserControl x:Class="FinanceApp.Views.Pages.TransactionsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок и кнопки -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0 0 0 15">
            <TextBlock Text="📋 Все операции" FontSize="28" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="➕ Добавить операцию" 
                    Background="#4CAF50" Foreground="White" FontSize="16"
                    Padding="20 8" Margin="30 0 0 0"
                    Click="BtnAdd_Click"/>
            <Button Content="✏️ Редактировать" Background="#2196F3" Foreground="White" 
        Padding="15 8" Margin="10 0 0 0" Click="BtnEdit_Click"/>
            <Button Content="🗑 Удалить" Background="#F44336" Foreground="White" 
        Padding="15 8" Margin="10 0 0 0" Click="BtnDelete_Click"/>
        </StackPanel>

        <!-- Фильтры (пока простые) -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 10">
            <ComboBox x:Name="cmbTypeFilter" Width="150" Margin="0 0 10 0" SelectedIndex="0">
                <ComboBoxItem Content="Все типы"/>
                <ComboBoxItem Content="Доход"/>
                <ComboBoxItem Content="Расход"/>
            </ComboBox>
            <Button Content="🔄 Обновить" Click="BtnRefresh_Click" Width="120"/>
        </StackPanel>

        <!-- Таблица операций -->
        <DataGrid x:Name="dgTransactions" Grid.Row="2" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single" CanUserResizeColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding TypeDisplay}" Width="90"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding Amount}" Width="100"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Category.Name}" Width="150"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="300"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>