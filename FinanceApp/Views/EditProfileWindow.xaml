<Window x:Class="FinanceApp.Views.EditProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование профиля" Height="661" Width="480"
        WindowStartupLocation="CenterOwner" 
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border CornerRadius="20" ClipToBounds="True">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#151229" Offset="0"/>
                <GradientStop Color="#1E1A3A" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.5" Color="#000000"/>
        </Border.Effect>

        <Grid Margin="32 28">
            <StackPanel>
                <!-- Title -->
                <Grid Margin="0 0 0 20">
                    <TextBlock Text="Профиль" Style="{StaticResource PageTitle}"/>
                    <Button Content="✕" HorizontalAlignment="Left" Width="49" Height="36"
                            FontSize="14" Foreground="{StaticResource TextMutedBrush}" 
                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                            Click="BtnCancel_Click" Margin="367,2,0,2"/>
                </Grid>

                <!-- Avatar -->
                <Border Width="100" Height="100" CornerRadius="50" 
                        BorderBrush="{StaticResource AccentBrush}" BorderThickness="3" 
                        HorizontalAlignment="Center" Margin="0 0 0 6">
                    <Border CornerRadius="47" ClipToBounds="True">
                        <Image x:Name="imgAvatar" Stretch="UniformToFill"/>
                    </Border>
                </Border>
                <Button Content="📸 Изменить фото" Style="{StaticResource GhostButton}" 
                        Width="169" Height="36" FontSize="13" Margin="0 6 0 20"
                        Click="BtnChangePhoto_Click"/>

                <TextBlock Text="ФИО" Style="{StaticResource Label}"/>
                <TextBox x:Name="txtFullName" Style="{StaticResource ModernTextBox}" Margin="0 0 0 16"/>

                <TextBlock Text="EMAIL" Style="{StaticResource Label}"/>
                <TextBox x:Name="txtEmail" Style="{StaticResource ModernTextBox}" Margin="0 0 0 16"/>

                <TextBlock Text="НОВЫЙ ПАРОЛЬ" Style="{StaticResource Label}"/>
                <TextBlock Text="Оставьте пустым, если не хотите менять" 
                           FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0 -3 0 6"/>
                <PasswordBox x:Name="txtNewPassword" Style="{StaticResource ModernPasswordBox}" Margin="0 0 0 24"/>

                <!-- Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Отмена" Style="{StaticResource GhostButton}" 
                            Width="120" Margin="0 0 12 0" Click="BtnCancel_Click"/>
                    <Button Content="Сохранить" Style="{StaticResource SuccessButton}" 
                            Width="160" Click="BtnSave_Click"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
