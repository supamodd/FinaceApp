<Window x:Class="FinanceApp.Views.AddTransactionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Новая операция"
        Height="720" Width="500"
        WindowStartupLocation="CenterScreen" 
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border CornerRadius="20" ClipToBounds="True">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#151229" Offset="0"/>
                <GradientStop Color="#1E1A3A" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.5" Color="#000000"/>
        </Border.Effect>

        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="32 28">
                <StackPanel>
                    <!-- Title Bar -->
                    <Grid Margin="0 0 0 24">
                        <TextBlock Text="Новая операция" Style="{StaticResource PageTitle}"/>
                        <Button Content="✕" HorizontalAlignment="Left" Width="49" Height="36"
                                FontSize="14" Foreground="{StaticResource TextMutedBrush}" 
                                Background="Transparent" BorderThickness="0" Cursor="Hand"
                                Click="BtnCancel_Click" Margin="387,2,0,2"/>
                    </Grid>

                    <TextBlock Text="ДАТА" Style="{StaticResource Label}"/>
                    <DatePicker x:Name="dpDate" SelectedDate="{x:Static sys:DateTime.Now}" Height="44" Margin="0 0 0 16"/>

                    <TextBlock Text="ТИП ОПЕРАЦИИ" Style="{StaticResource Label}"/>
                    <ComboBox x:Name="cmbType" Height="44" FontSize="15" Margin="0 0 0 16">
                        <ComboBoxItem Content="Расход" IsSelected="True" Tag="Expense"/>
                        <ComboBoxItem Content="Доход" Tag="Income"/>
                    </ComboBox>

                    <TextBlock Text="СУММА" Style="{StaticResource Label}"/>
                    <TextBox x:Name="txtAmount" Style="{StaticResource ModernTextBox}" Margin="0 0 0 16"/>

                    <TextBlock Text="КАТЕГОРИЯ" Style="{StaticResource Label}"/>
                    <ComboBox x:Name="cmbCategory" Height="44" DisplayMemberPath="Name" FontSize="15" Margin="0 0 0 16"/>

                    <TextBlock Text="ОПИСАНИЕ" Style="{StaticResource Label}"/>
                    <TextBox x:Name="txtDescription" Style="{StaticResource ModernTextBox}" 
                             Height="80" TextWrapping="Wrap" AcceptsReturn="True" 
                             VerticalContentAlignment="Top" Margin="0 0 0 16"/>

                    <!-- Recurring -->
                    <CheckBox x:Name="chkRecurring" Content="🔄 Повторять автоматически" 
                              Margin="0 8 0 10" FontSize="14" 
                              Foreground="{StaticResource TextSecondaryBrush}"
                              Checked="ChkRecurring_Changed" Unchecked="ChkRecurring_Changed"/>

                    <StackPanel x:Name="RecurringPanel" Visibility="Collapsed" Margin="0 0 0 16">
                        <TextBlock Text="ЧАСТОТА" Style="{StaticResource Label}"/>
                        <ComboBox x:Name="cmbFrequency" Height="44" FontSize="14" Margin="0 0 0 12">
                            <ComboBoxItem Content="Ежемесячно" IsSelected="True" Tag="Monthly"/>
                            <ComboBoxItem Content="Еженедельно" Tag="Weekly"/>
                            <ComboBoxItem Content="Раз в 2 недели" Tag="BiWeekly"/>
                            <ComboBoxItem Content="Ежегодно" Tag="Yearly"/>
                        </ComboBox>

                        <Grid Margin="0 0 0 12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0 0 8 0">
                                <TextBlock Text="НАЧАЛО" Style="{StaticResource Label}"/>
                                <DatePicker x:Name="dpStartDate" Height="42"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="8 0 0 0">
                                <TextBlock Text="КОНЕЦ" Style="{StaticResource Label}"/>
                                <DatePicker x:Name="dpEndDate" Height="42"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="ДЕНЬ МЕСЯЦА" Style="{StaticResource Label}"/>
                        <ComboBox x:Name="cmbDayOfMonth" Height="44" FontSize="14"/>
                    </StackPanel>

                    <!-- Buttons -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 16 0 0">
                        <Button Content="Отмена" Style="{StaticResource GhostButton}" 
                                Width="120" Margin="0 0 12 0" Click="BtnCancel_Click"/>
                        <Button Content="Сохранить" Style="{StaticResource SuccessButton}" 
                                Width="160" Click="BtnSave_Click"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Border>
</Window>
